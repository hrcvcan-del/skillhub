<%- include('../partials/header', { title: typeof title !== 'undefined' ? title : 'Access denied' }) %>
<div class="text-center py-5">
  <h1 class="display-1">403</h1>
  <p class="lead"><%= typeof message !== 'undefined' && message ? message : "You don't have permission to access this page." %></p>
  <a href="/dashboard" class="btn btn-primary">Go to dashboard</a>
</div>
<%- include('../partials/footer') %>
